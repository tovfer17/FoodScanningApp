const fetch = require('node-fetch')
const assert = require('assert')

const url = 'http://localhost:3000/store'

describe('Dummy Store Test',() => {
    it('Passes',() => {
        assert.strictEqual(true,true)
    })
})